export const config = {
  API_URL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000',
  TEST_USER: process.env.TEST_USER || 'medardmtest',
  TEST_USER_PASSWORD: process.env.TEST_USER_PASSWORD || '@password@',
}
